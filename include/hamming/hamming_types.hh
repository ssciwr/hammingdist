#pragma once

#include <algorithm>
#include <array>
#include <cmath>
#include <cstdint>
#include <fstream>
#include <string>
#include <vector>

namespace hamming {

using ReferenceDistIntType = uint32_t;
using DefaultDistIntType = uint8_t;

} // namespace hamming
